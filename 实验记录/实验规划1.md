 对 --discrete-to-text 做完整消融实验，明确 discrete-to-text 模式下各组件的作用。所有对比使用相同的 85 变量配置和
    support→query 协议。

    基准对比（最优先）：
    1. discrete-to-text OFF，L=64, Layer 12, projector=direct, vocab=0 — 3 seeds (2026/2027/2028)
    2. discrete-to-text ON，L=64, Layer 12, projector=direct, vocab=0 — 3 seeds

    第1组如果已有结果可直接引用旧 dtt 之前的 baseline（需确认是 85 变量协议下的结果，否则重跑）。第2组之前有严重 batch
    bug，bug 修复后已清除了旧结果，需重跑。

窗口长度消融（discrete-to-text ON, Layer 12, projector=direct, vocab=0）：
    3. L=8 — 1 seed (2026)
    4. L=16 — 1 seed (2026)
    5. L=32 — 1 seed (2026)
    6. L=64 — 引用第2组的 seed 2026
    7. L=128 — 1 seed (2026)

    如果某窗口 PR-AUC 接近或超过 L=64，再补跑该窗口的 3 种子。

    MOIRAI 层消融（discrete-to-text ON, L=64, projector=direct, vocab=0）：
    8. Layer 10 — 3 seeds
    9. Layer 12 — 引用第2组

    旧的非 dtt 层扫描显示第10层是第二名胜者，验证在 dtt 模式下是否仍是第12层最优。

    映射层消融（discrete-to-text ON, L=64, Layer 12, vocab=0）：
    10. projector=linear — 3 seeds
    11. projector=direct — 引用第2组
    12. projector=reprogramming — 1 seed (2026)，若 PR-AUC ≥ direct 则补 3 seeds

    损失消融（discrete-to-text ON, L=64, Layer 12, projector=direct）：
    13. vocab=0.1, classifier=1 — 3 seeds（弱词表辅助）
    14. vocab=1, classifier=1 — 3 seeds（等权词表辅助）
    15. vocab=1, classifier=0 — 3 seeds（纯 verbalizer，冻结分类头）

     运行名格式：dtt_abl_<变量>_<值>_seed<种子>，如
    dtt_abl_L_32_seed2026、dtt_abl_layer_10_seed2026、dtt_abl_proj_linear_seed2026。

    汇总要求：所有实验完成后写入 record/dtt_ablation/README.md，每组报告三 seed 的 PR-AUC/F1/FPR 均值±标准差（单 seed
    的只报数值不加 std）。对每个消融维度做一个对比小节，指出最优配置。

    顺序建议：先跑第1、2组基准对比，确认 discrete-to-text 的基础效果后再跑后续消融。若基准对比中 dtt ON 明显差于
    OFF，仍应完成全部消融以确定瓶颈在哪个组件。