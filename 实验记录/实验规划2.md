对 --projector、--vocab-loss-weight、--classifier-loss-weight 做完整的三种子消融实验。固定参数：L=64、Layer
    12、prompt-variant=process、device=cuda。

    需要跑的组合，每个组合跑 seed 2026/2027/2028：

    Projector 消融（vocab_loss_weight=0, classifier_loss_weight=1）：
    1. --projector linear — 单层 768→1024
    2. --projector direct — 768→1536→1024 MLP（当前基线，可能已有结果可直接用）
    3. --projector reprogramming — 原型交叉注意力

    Vocab 损失消融（projector=direct, classifier_loss_weight=1）：
    4. --vocab-loss-weight 0 — 纯分类头（基线，可能已有）
    5. --vocab-loss-weight 0.1 — 弱词表辅助
    6. --vocab-loss-weight 1.0 — 等权词表辅助

    纯 Verbalizer 消融（projector=direct）：
    7. --classifier-loss-weight 0 --vocab-loss-weight 1 — 纯词表输出，冻结分类头

    注意事项：
    - 如果某些组合已有三种子结果（如 direct baseline），不要重复跑，在汇总时直接引用已有结果
    - 每组运行名带上组件名和 seed，如 grid_projector_linear_2026、grid_vocab01_2026
    - 跑完后按 PR-AUC 均值±标准差汇总到 实验记录.md，列出每组合的三 seed
    PR-AUC、F1、FPR，并比较：linear vs direct vs reprogramming 的差异、vocab 辅助是否提升主分类头、纯 verbalizer
    与分类头的差距
    - 如果需要先跑还没跑的基线组合，优先补基线的三 seed，再跑消融
